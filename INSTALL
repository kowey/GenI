Requirements
------------
 * ghc 6.8 or 6.10
 * libgmp (for ghc)
 * wxhaskell 0.10 (darcs version for now)
 * wxWidgets 2.8 (for wxhaskell 0.11)
 * graphviz (for GUI) 

Building GenI
--------------
1. obtain cabal-install

2. cd libGenI
   cabal configure
   cabal build
   cabal install

3. cd GenI
   cabal configure
   cabal build
   cabal install

You could also try the makefile
